export class BaseRenderPass {
    constructor(device, context) {   
    }

    onBeginEncode(encoder: GPUCommandEncoder) {
        encoder.beginRenderPass({
            
        })
    }

    encode(encoder: GPUCommandEncoder) {
        encoder.
    }
}